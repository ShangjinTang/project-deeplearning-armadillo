# C++ Deep Learning with Armadillo Framework

## How to build from command line

The project requires built using `conan` with `pipx`:

```bash
python3 -m pip install pipx
pipx ensurepath
pipx install conan
./compile_run.sh
```
